---
name: "runtime-truth"
gate_level: 2
evidence_max: 4
cheapest_proof: "runtime_identity.json + runtime_truth_check exit 0"
escalate_when: "Browser verification can escalate to level 2"
description: "Capture and verify runtime environment identity (OS, deps, container, ports, services)"
when_to_use:
  - "Before any user-facing acceptance"
  - "Before regression forensics"
  - "After deployment or container rebuild"
  - "When runtime_identity.json is stale or missing"
required_inputs:
  - "Target service/process to verify"
  - "Trusted endpoint expected by the operator or deployment configuration"
  - "Expected runtime_identity.json (if exists)"
step_by_step:
  - name: "Capture runtime proof"
    command: "python3 scripts/statedd_runtime_proof.py --evidence-dir docs/evidence/<slice> --url <endpoint>"
    expected: "Creates/updates runtime_identity.json with OS, kernel, deps, container, ports, services, git head"
    failure: "Check script output, fix environment detection"
  - name: "Verify against recorded"
    command: "python3 scripts/statedd_runtime_truth_check.py --artifact docs/evidence/<slice>/runtime_identity.json --expected-endpoint <endpoint>"
    expected: "Exit 0 (current runtime matches recorded)"
    failure: "Update runtime_identity.json or investigate drift"
  - name: "Browser verification (if user-facing)"
    command: "python scripts/statedd_browser_verify.py --url <endpoint>"
    expected: "Exit 0 (browser loads, captures screenshot, verifies behavior)"
    failure: "Check endpoint, browser tooling, network"
  - name: "Record evidence"
    action: "Add runtime_identity.json and browser artifacts to evidence bundle"
    verify: "Evidence bundle per EVIDENCE_README_TEMPLATE.md"
expected_outputs:
  - "runtime_identity.json (current)"
  - "Browser screenshot/log (if user-facing)"
  - "Runtime truth check output"
failure_cases:
  - name: "Runtime proof script fails"
    detection: "statedd_runtime_proof.py exits non-zero"
    recovery: "Check script output, fix environment detection"
    evidence: "Script output, logs"
  - name: "Runtime identity mismatch"
    detection: "statedd_runtime_truth_check.py exits non-zero"
    recovery: "Update runtime_identity.json or investigate drift"
    evidence: "Truth check output"
  - name: "Browser verification fails"
    detection: "statedd_browser_verify.py exits non-zero"
    recovery: "Check endpoint, browser tooling, network; use fallback"
    evidence: "Fallback evidence, provider used"
evidence_required:
  - "runtime_identity.json path"
  - "Browser verification artifacts (screenshots, logs)"
  - "Runtime truth check stdout"
exit_criteria:
  - "Runtime identity captured and verified"
  - "Browser verification complete (if applicable)"
  - "Evidence recorded in bundle"
