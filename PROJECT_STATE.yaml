# PROJECT_STATE.yaml - Structured current truth

metadata:
  updated_at: 2026-07-10T14:34:03+02:00
  updated_by: agent
  version: "statedd-template-v5"

workflow:
  repo_role: template_repository
  statedd_mode: template-maintenance
  repo_mode: template-maintenance
  delivery_state: stabilization
  template_maintenance:
    status: observed
    purpose: maintain and release the reusable StateDD template repository
    downstream_default_role: downstream_project
    downstream_default_mode: bootstrap
    queued_slice: BL-CORE-001
    feature_freeze: true
    feature_freeze_exit: BL-CORE-001 merged and post-merge verified

verification_labels:
  observed: verified directly in the named session and truth boundary
  unknown: not yet determined from available evidence
  reported: supplied by an audit or prior evidence but not reproduced locally
  blocked: verification attempted but prevented
  assumed: provisional working assumption
  stale: previously verified but no longer fresh for the current head
  invalid: known false or superseded

current_state:
  execution_mode:
    status: observed
    mode: template-maintenance
    delivery_state: stabilization
    notes: |
      Repository mode remains template-maintenance. Stabilization is a delivery
      posture, not a new repository mode. Feature work is held until BL-CORE-001
      is merged and post-merge verified.

  audit_intake:
    id: upstream-audit-2026-07-10
    source: user_supplied_upstream_audit
    scope: origin main, draft PR 4, closure/generator/state/update/tooling contracts
    source_claim_status: reported
    planning_status: integrated
    implementation_status: locally_implemented_preflight_passed_uncommitted
    accepted_direction: stabilization_before_features
    privacy_boundary: |
      Personal StudyDD observations are accepted only as abstract failure
      classes. No learner state, answers, targets, identifiers, private URLs,
      or private evidence may enter this public repository.
    directly_observed:
      - id: OBS-MAIN-STATE-STALE
        status: observed
        claim: origin/main state still prioritizes BL-BROWSER-002 and reports no known P0/P1 failures
        boundary: remote_branch_tree
      - id: OBS-PR-BODY-STALE
        status: observed
        claim: draft PR 4 head is 976a3f0 while its body names 0c2a136 and dadf4ad as proof/final heads
        boundary: github_pr
      - id: OBS-PR-CI-LIMITED
        status: observed
        claim: current-head GitHub checks succeeded, but the visible workflow is documentation validation and does not prove every PR-body claim
        boundary: github_ci
      - id: OBS-VERSION-DRIFT
        status: observed
        claim: VERSION is statedd-template-v5 while CHANGELOG.md stops at v4
        boundary: remote_branch_tree
      - id: OBS-PUBLIC-MACHINE-IDENTITY
        status: observed
        claim: prior PROJECT_STATE tracked an absolute home path, kernel, and local runtime details
        boundary: remote_branch_tree
    reported_pending_reproduction:
      - id: AUDIT-REMOTE-TRUTH
        claim: remote-truth logic can treat a dirty worktree as clean and overstate GitHub verification
      - id: AUDIT-HANDOFF-EXIT
        claim: failed handoff test commands can still yield process exit zero
      - id: AUDIT-GATE-CONFIG
        claim: missing test or lint infrastructure can be classified as success
      - id: AUDIT-RUNTIME-EVIDENCE
        claim: runtime and evidence gates consume inconsistent artifact models and global history
      - id: AUDIT-DUPLICATE-YAML
        claim: downstream DNA generation emits duplicate top-level keys that validation silently overwrites
      - id: AUDIT-VERSION-DETECTION
        claim: template version validation depends on an obsolete exact README heading
      - id: AUDIT-UPGRADE-REPORT
        claim: applied upgrades can still emit dry_run true
      - id: AUDIT-PRIVACY
        claim: runtime evidence bypasses meaningful public machine-identity and PII scanning
      - id: AUDIT-CLOSURE-SELF-REFERENCE
        claim: tracked evidence and mutable PR prose create a self-referential final-head loop
      - id: AUDIT-CONTROL-PLANE-DUPLICATION
        claim: scripts, prompts, skills, commands, schemas, catalogs, and state independently encode drifting workflow rules

  quality_gates:
    status: degraded_pending_stabilization
    product_quality_gate: not_applicable_for_template_root
    runtime_truth_gate: audit_reported_conflict_pending_reproduction
    live_canary_gate: not_applicable_for_template_root
    redteam_gate: stale_prior_result_not_sufficient_for_current_claims
    known_bad_events_gate: audit_reported_failure_classes_missing_full_regression_coverage
    remote_closure_gate:
      status: invalidated_for_current_closure_claims
      current_script: scripts/statedd_remote_closure_finalizer.py
      reason: current PR head, PR prose, tracked evidence, and claimed proof heads do not agree
      policy: no existing closure script may elevate BL-CORE-001 without its corrected negative tests
    validation_semantics:
      required_results:
        - PASS
        - FAIL
        - NOT_CONFIGURED
        - NOT_APPLICABLE
        - BLOCKED
        - SKIPPED_BY_EXPLICIT_OVERRIDE
      invariant: only PASS crosses a required local validation boundary
      implementation_status: backlog_only

  release_posture:
    status: blocked_on_stabilization
    release_type: stabilization_not_feature
    blockers:
      - BL-CORE-001 is not implemented
      - draft PR 4 is not closure-grade at its current head
      - reported false-pass conditions lack focused failure-injection proof
    resume_features_when:
      - BL-CORE-001 exact-head CI passes
      - BL-CORE-001 is merged
      - post-merge verification succeeds on main

  repository:
    canonical_path: "$REPO_ROOT"
    path_status: unknown
    path_representation: public_normalized
    path_truth: intentionally_not_persisted_in_public_state
    public_privacy_profile: public
    git_snapshot:
      status: observed
      captured_at: 2026-07-10T14:34:03+02:00
      capture_tools:
        - git
        - gh
      default_branch: main
      default_branch_head: 81257e6119877b200873585c0e4d71c62ef6d4ed
      draft_pr:
        number: 4
        state: open_draft
        branch: bl-workflow-002-worktree-brittleness
        head: 976a3f0e2a38ba7bf096a300db16b95b65bd53f4
        merge_state_at_capture: CLEAN
        current_head_ci_at_capture: success_for_visible_docs_checks
        body_proof_head: 0c2a13639894cd799506a9a5e94ecb1f3a070ffe
        body_final_head: dadf4ad1afd6faa6aa91fd417a5e8f33d87b5f5e
        body_alignment: invalid
        closure_grade: false
      notes: |
        Git cleanliness and head equality are decision-time facts and are not
        stored as timeless clean claims. Re-run Git and GitHub checks before any
        transition across local, remote, PR, CI, or merge truth boundaries.

  operating_mode:
    status: observed
    mode: template-maintenance
    summary: maintain the upstream template while executing a stabilization-only queue

  project:
    name: StateDD_Template
    type: documentation_and_workflow_template
    lifecycle_stage: template-maintenance_stabilization
    truth_summary: critical_correctness_repair_queued

  runtime_truth:
    status: not_applicable_for_template_root
    downstream_contract_status: audit_reported_conflict_pending_reproduction
    notes: |
      The template root has no application runtime. Runtime proof remains
      required for downstream user-facing closure, but the artifact contract
      and its evidence discovery must be reconciled before reuse is trusted.

  runtime_identity:
    status: not_applicable_for_template_root
    repo_path: "$REPO_ROOT"
    public_representation: normalized
    artifact_contract: audit_reported_conflict_pending_reproduction
    helper: scripts/statedd_runtime_proof.py
    process_or_container: no application runtime
    port_or_base_url: not_applicable
    notes: do not commit hostnames, PIDs, command lines, or absolute home paths in public evidence

  environment:
    status: intentionally_not_recorded_in_public_state
    privacy_profile: public
    local_details_location: ignored local or CI receipt when a gate requires them

  known_bad_events:
    status: audit_reported_failure_classes
    reproduction_status: scoped_false_passes_reproduced_with_negative_regressions
    fixture_policy: every reproduced false pass becomes a focused negative regression

  open_p0_failures:
    - BL-CORE-001 false closure and generator corruption

  closure_blockers:
    - BL-CORE-001 not implemented, merged, or post-merge verified
    - draft PR 4 proof metadata does not match its current head
    - current CI does not reproduce every claimed runtime/evidence/privacy/closure condition

  last_live_canary:
    status: not_applicable_for_template_root
    evidence: null

  last_redteam_run:
    status: stale
    reason: prior passing result predates the 2026-07-10 audit and does not cover its failure classes

  residual_risks:
    - audit findings are not exhaustive and most still require local reproduction
    - downstream repositories may continue consuming stale main until a corrected release propagates
    - draft PR 4 mixes multiple concerns and should not be treated as a focused repair slice
    - manual asset and instruction catalogs remain drift-prone until their backlog slices land

  evidence:
    status: degraded_for_closure
    ledger: docs/EVIDENCE_LOG.md
    artifact_root: docs/evidence
    current_rule: evidence must be bound to the active slice and exact head
    current_limitation: existing historical bundles are not proof for draft PR 4 current head

  documentation:
    status: state_corrected_locally
    canonical_live_state:
      - STATUS.md
      - PROJECT_STATE.yaml
      - PROJECT_DNA.yaml
      - NEXT_ACTIONS.md
      - BACKLOG.md
    history:
      - WORKLOG.md
    dynamic_discovery_rule: use repository discovery until BL-UPSTREAM-001 creates the authoritative managed-asset manifest

active_problems:
  - id: BL-CORE-001
    title: Eliminate false closure and generator corruption
    severity: P0
    state: active
    implementation_status: locally_implemented_preflight_passed_uncommitted
    source: upstream-audit-2026-07-10
    acceptance_contract: BACKLOG.md NOW section
    next_action_contract: NEXT_ACTIONS.md
    non_goals:
      - parallel-agent orchestration
      - browser-provider integration
      - broad instruction rewrites
      - managed downstream updates
      - toolpacks and model routing
      - full package migration
    closure_requirement: merged and post-merge verified on exact current head
