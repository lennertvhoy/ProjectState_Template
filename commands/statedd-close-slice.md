---
alias_for: projectstate-close-slice
status: backward-compat (one migration cycle)
---

# Alias: /statedd-close-slice

This is a backward-compat alias. The canonical command is now
[`/projectstate-close-slice`](projectstate-close-slice.md).

Invoke `/projectstate-close-slice` instead. See that file for the full playbook.
