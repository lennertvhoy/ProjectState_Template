---
alias_for: projectstate-git-safety
status: backward-compat (one migration cycle)
---

# Alias: /statedd-git-safety

This is a backward-compat alias. The canonical command is now
[`/projectstate-git-safety`](projectstate-git-safety.md).

Invoke `/projectstate-git-safety` instead. See that file for the full playbook.
