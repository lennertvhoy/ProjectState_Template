---
alias_for: projectstate-failure-scan
status: backward-compat (one migration cycle)
---

# Alias: /statedd-failure-scan

This is a backward-compat alias. The canonical command is now
[`/projectstate-failure-scan`](projectstate-failure-scan.md).

Invoke `/projectstate-failure-scan` instead. See that file for the full playbook.
