---
alias_for: projectstate-quality-freeze
status: backward-compat (one migration cycle)
---

# Alias: /statedd-quality-freeze

This is a backward-compat alias. The canonical command is now
[`/projectstate-quality-freeze`](projectstate-quality-freeze.md).

Invoke `/projectstate-quality-freeze` instead. See that file for the full playbook.
