---
alias_for: projectstate-remote-closure
status: backward-compat (one migration cycle)
---

# Alias: /statedd-remote-closure

This is a backward-compat alias. The canonical command is now
[`/projectstate-remote-closure`](projectstate-remote-closure.md).

Invoke `/projectstate-remote-closure` instead. See that file for the full playbook.
